@model IEnumerable<TaskCollector.Contract.Model.Client>

<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(s => s.Id)</th>
           
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>                
                
                <td>
                    @Html.ActionLink("Редактировать", "Details", "Client", new { id = item.Id }, new { @class = "btn btn-dark" })
                    @Html.ActionLink("Удалить", "Delete", "Client", new { id = item.Id }, new { @class = "btn btn-dark" })
                </td>
            </tr>
        }
    </tbody>
</table>
